Centralized design-system source.

- `tokens/` keeps typed design tokens (color, spacing, radius, typography, motion, shadow)
- `styles/` maps tokens into global CSS variables and primitive styles
